/ Step 1: Use `publishConfig` option in your package.json
"publishConfig": { "registry": "https://github.hubspot.com/" }
// Step 2: Authenticate
$ npm login --registry=https://https://github.hubspot.com//

// Step 3: Publish
$ npm publish
